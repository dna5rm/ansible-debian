# This file holds shared secrets or RSA private keys for authentication.

{% for item in interface|list %}{% if item.startswith('vti') %}
{% set intf = interface | json_query(item) %}
{{ intf.remote }} : PSK "{{ intf.psk }}"
{% endif %}{% endfor %}
